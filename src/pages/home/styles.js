import { makeStyles } from '@material-ui/core/styles';

const styles = makeStyles(() => ({
  container: {
    paddingTop: '5%',
    marginLeft: '10%',
    marginRight: '10%',
  },
}));

export default styles;
