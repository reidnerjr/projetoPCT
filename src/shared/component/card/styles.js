import { makeStyles } from '@material-ui/core/styles';

const styles = makeStyles( {
    root: {
        marginTop:  10,
        maxWidth: 345,
    },
    botao: {
        background:'red'
    }
  
});

export default styles;